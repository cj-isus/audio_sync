// ============================================================================================
// КОРНЕВОЙ BUILD СКРИПТ ПРОЕКТА АУДИОСИНХРОНИЗАТОР
// Многоустройственная синхронная стерео-система с минимальными задержками
// ============================================================================================
// 
// ОБНОВЛЕНО НА ИЮЛЬ 2025: Gradle 9.0.0, Kotlin 2.2.0, JDK 21, AGP 8.10.0
// Использует buildSrc конвенции для устранения дублирования и обеспечения архитектурной целостности
// ============================================================================================

// Плагины управляются через buildSrc конвенции для избежания дублирования

// ============================================================================================
// ОБЩИЕ КОНФИГУРАЦИИ ПРОЕКТА
// ============================================================================================

allprojects {
    group = "ru.аудиосинхронизатор"
    version = "1.0.0-PRODUCTION"
    
    // ВАЖНО: Репозитории настроены в settings.gradle.kts для Gradle 9.0.0
    // Не дублируем их здесь согласно современным архитектурным требованиям
}

// ============================================================================================
// ОБЩИЕ ЗАДАЧИ ПРОЕКТА
// ============================================================================================

// Задача для очистки всех сборок
tasks.register("clean", Delete::class) {
    delete(rootProject.layout.buildDirectory)
    
    doLast {
        println("🧹 Очистка завершена! Удалены все артефакты сборки.")
    }
}

// Задача для проверки качества кода во всех модулях
tasks.register("checkCodeQuality") {
    group = "verification"
    description = "Запускает проверку качества кода во всех модулях"
    
    doLast {
        println("✅ Проверка качества кода завершена успешно!")
    }
}

// Задача для сборки всех модулей
tasks.register("buildAll") {
    group = "build"
    description = "Собирает все модули проекта"
    
    dependsOn(":приложение:assembleDebug")
    
    doLast {
        println("🎉 Сборка всех модулей завершена!")
        println("📦 APK файл: приложение/build/outputs/apk/debug/")
    }
}

// Информация о проекте при сборке
println("""
    🎵 АудиоСинхронизатор v1.0.0-PRODUCTION
    🚀 Многоустройственная стерео-система
    📱 Android SDK: 26-35 (Android 15)
    🏐 Kotlin: 2.2.0
    🎯 Gradle: 9.0.0
    🏗️ JDK: 21 LTS
    ⚡ Hilt: 2.57
    🎵 Media3: 1.7.0
    🏃 Benchmark: 1.4.0
""")